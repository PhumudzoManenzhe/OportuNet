{
  "name": "oportunet",
  "version": "1.0.0",
  "private": true,
  "description": "OportuNet JavaScript project test tooling",
  "scripts": {
    "test": "jest --runInBand",
    "test:coverage": "jest --coverage --runInBand",
    "coverage": "jest --coverage --runInBand"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
